# TODO (minor, not blocking)

- [ ] Add a favicon so the tab is easy to find
- [ ] Agent appears at desk 0 instead of the desk that was clicked
- [ ] (+) icon on empty desk hover appears off-center briefly before snapping into place
- [ ] Investigate: some git commands blocked even with bypassPermissions (Claude Code built-in security rules for compound cd&&git and --git-dir flags)
